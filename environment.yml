name: mie429

channels:
  - pytorch
  - conda-forge

dependencies:
  - python=3.10
  - pip
  - matplotlib
  - notebook
  - cudatoolkit=11.6
  - pytorch=1.12.1
  - torchvision
  - pandas
  - scipy
  - numpy
  - pynetdicom
  - conda-forge::torchdata # pytorch channel uses never version of glibc

  - pip:
    - flake8
    - opencv-python-headless
    - tqdm
    - pyyaml
